<UserControl x:Class="WATA.LIS.Main.Views.TopBarUI"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:i ="http://schemas.microsoft.com/expression/2010/interactivity"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d" 
             d:DesignHeight="120" d:DesignWidth="1280">
    <UserControl.Resources>
        <ResourceDictionary>
            <!-- App spacing & radius tokens -->
            <Thickness x:Key="AppOuterMargin">8,6,8,6</Thickness>
            <CornerRadius x:Key="AppCornerRadius">10</CornerRadius>
            <sys:Double x:Key="TopBarKeyWidth">100</sys:Double>
            
            <!-- Card-like container with rounded corners for top bar -->
            <Style x:Key="TopBarCard" TargetType="Border">
                <Setter Property="CornerRadius" Value="{StaticResource AppCornerRadius}"/>
                <Setter Property="Background" Value="#FFFFFF"/>
                <Setter Property="Padding" Value="8,10"/>
                <Setter Property="Margin" Value="{StaticResource AppOuterMargin}"/>
                <Setter Property="VerticalAlignment" Value="Top"/>
                <Setter Property="SnapsToDevicePixels" Value="True"/>
                <Setter Property="Effect">
                    <Setter.Value>
                        <DropShadowEffect BlurRadius="8" Direction="270" ShadowDepth="2" Opacity="0.12"/>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- Fixed-size topbar label styles (readable, compact) -->
            <Style x:Key="TopBarKeyLabel" TargetType="Label">
                <Setter Property="FontSize" Value="4"/>
                <Setter Property="FontWeight" Value="Bold"/>
                <Setter Property="Foreground" Value="#7A8A99"/>
                <Setter Property="VerticalAlignment" Value="Center"/>
                <Setter Property="HorizontalAlignment" Value="Left"/>
                <Setter Property="HorizontalContentAlignment" Value="Right"/>
                <Setter Property="Width" Value="{StaticResource TopBarKeyWidth}"/>
                <Setter Property="Height" Value="30"/>
                <Setter Property="Margin" Value="0"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Label">
                            <Border Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    Padding="{TemplateBinding Padding}">
                                <ContentPresenter Content="{TemplateBinding Content}"
                                                  ContentTemplate="{TemplateBinding ContentTemplate}"
                                                  ContentStringFormat="{TemplateBinding ContentStringFormat}"
                                                  HorizontalAlignment="Right"
                                                  VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
            <Style x:Key="TopBarValueLabel" TargetType="Label">
                <Setter Property="FontSize" Value="4"/>
                <Setter Property="FontWeight" Value="Bold"/>
                <Setter Property="Foreground" Value="#334155"/>
                <Setter Property="VerticalAlignment" Value="Center"/>
                <Setter Property="HorizontalAlignment" Value="Left"/>
                <Setter Property="HorizontalContentAlignment" Value="Left"/>
                <Setter Property="Height" Value="30"/>
                <Setter Property="Margin" Value="0"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Label">
                            <Border Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    Padding="{TemplateBinding Padding}">
                                <ContentPresenter Content="{TemplateBinding Content}"
                                                  ContentTemplate="{TemplateBinding ContentTemplate}"
                                                  ContentStringFormat="{TemplateBinding ContentStringFormat}"
                                                  HorizontalAlignment="Left"
                                                  VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- existing blink styles unchanged -->
            <Style x:Key="btnBlink" TargetType="Button" >
                <Style.Triggers>
                    <DataTrigger Binding="{Binding RobotStatusManager.IsOnAlarm}" Value="true">
                        <DataTrigger.EnterActions>
                            <BeginStoryboard x:Name="sb1">
                                <BeginStoryboard.Storyboard>
                                    <Storyboard AutoReverse="True" RepeatBehavior="Forever">
                                        <ColorAnimationUsingKeyFrames Storyboard.TargetProperty="(Panel.Background).(SolidColorBrush.Color)" >
                                            <EasingColorKeyFrame KeyTime="0" Value="Lime"/>
                                            <EasingColorKeyFrame KeyTime="0:0:1" Value="#FF75002A">
                                                <EasingColorKeyFrame.EasingFunction>
                                                    <QuinticEase EasingMode="EaseInOut"/>
                                                </EasingColorKeyFrame.EasingFunction>
                                            </EasingColorKeyFrame>
                                        </ColorAnimationUsingKeyFrames>
                                    </Storyboard>
                                </BeginStoryboard.Storyboard>
                            </BeginStoryboard>
                        </DataTrigger.EnterActions>
                        <DataTrigger.ExitActions>
                            <StopStoryboard BeginStoryboardName="sb1"/>
                        </DataTrigger.ExitActions>
                    </DataTrigger>
                    <Trigger Property="IsEnabled" Value="false">
                        <Setter Property="Background" Value="#ff0051a5"/>
                    </Trigger>
                </Style.Triggers>
            </Style>
            <Style x:Key="lbBlink" TargetType="Label" >
                <Style.Triggers>
                    <DataTrigger Binding="{Binding RobotStatusManager.IsRunning}" Value="true">
                        <DataTrigger.EnterActions>
                            <BeginStoryboard x:Name="sb1">
                                <BeginStoryboard.Storyboard>
                                    <Storyboard AutoReverse="True" RepeatBehavior="Forever">
                                        <ColorAnimationUsingKeyFrames Storyboard.TargetProperty="(Label.Foreground).(SolidColorBrush.Color)" >
                                            <EasingColorKeyFrame KeyTime="0" Value="Lime"/>
                                            <EasingColorKeyFrame KeyTime="0:0:0.8" Value="#FF75002A">
                                                <EasingColorKeyFrame.EasingFunction>
                                                    <QuinticEase EasingMode="EaseInOut"/>
                                                </EasingColorKeyFrame.EasingFunction>
                                            </EasingColorKeyFrame>
                                        </ColorAnimationUsingKeyFrames>
                                    </Storyboard>
                                </BeginStoryboard.Storyboard>
                            </BeginStoryboard>
                        </DataTrigger.EnterActions>
                        <DataTrigger.ExitActions>
                            <StopStoryboard BeginStoryboardName="sb1"/>
                        </DataTrigger.ExitActions>
                    </DataTrigger>
                    <Trigger Property="IsEnabled" Value="false">
                        <Setter Property="Foreground" Value="#ff0051a5"/>
                    </Trigger>
                </Style.Triggers>
            </Style>
            <Style x:Key="lbBlink2" TargetType="Label" >
                <Style.Triggers>
                    <DataTrigger Binding="{Binding RobotStatusManager.AutoManual}" Value="true">
                        <DataTrigger.EnterActions>
                            <BeginStoryboard x:Name="sb1">
                                <BeginStoryboard.Storyboard>
                                    <Storyboard AutoReverse="True" RepeatBehavior="Forever">
                                        <ColorAnimationUsingKeyFrames Storyboard.TargetProperty="(Label.Foreground).(SolidColorBrush.Color)" >
                                            <EasingColorKeyFrame KeyTime="0" Value="Lime"/>
                                            <EasingColorKeyFrame KeyTime="0:0:0.8" Value="#FF75002A">
                                                <EasingColorKeyFrame.EasingFunction>
                                                    <QuinticEase EasingMode="EaseInOut"/>
                                                </EasingColorKeyFrame.EasingFunction>
                                            </EasingColorKeyFrame>
                                        </ColorAnimationUsingKeyFrames>
                                    </Storyboard>
                                </BeginStoryboard.Storyboard>
                            </BeginStoryboard>
                        </DataTrigger.EnterActions>
                        <DataTrigger.ExitActions>
                            <StopStoryboard BeginStoryboardName="sb1"/>
                        </DataTrigger.ExitActions>
                    </DataTrigger>

                    <Trigger Property="Content" Value="">
                        <Setter Property="Foreground" Value="#ff0051a5"/>
                    </Trigger>
                </Style.Triggers>
            </Style>
            <Style x:Key="lbBlink1" TargetType="Label" >
                <Style.Triggers>
                    <DataTrigger Binding="{Binding ProcessingJob}" Value="true">
                        <DataTrigger.EnterActions>
                            <BeginStoryboard x:Name="sb1">
                                <BeginStoryboard.Storyboard>
                                    <Storyboard AutoReverse="True" RepeatBehavior="Forever">
                                        <ColorAnimationUsingKeyFrames Storyboard.TargetProperty="(Label.Background).(SolidColorBrush.Color)" >
                                            <EasingColorKeyFrame KeyTime="0" Value="#ff0051a5"/>
                                            <EasingColorKeyFrame KeyTime="0:0:0.8" Value="White">
                                                <EasingColorKeyFrame.EasingFunction>
                                                    <QuinticEase EasingMode="EaseInOut"/>
                                                </EasingColorKeyFrame.EasingFunction>
                                            </EasingColorKeyFrame>
                                        </ColorAnimationUsingKeyFrames>
                                        <ColorAnimationUsingKeyFrames Storyboard.TargetProperty="(Label.Foreground).(SolidColorBrush.Color)" >
                                            <EasingColorKeyFrame KeyTime="0" Value="White"/>
                                            <EasingColorKeyFrame KeyTime="0:0:0.8" Value="Black">
                                                <EasingColorKeyFrame.EasingFunction>
                                                    <QuinticEase EasingMode="EaseInOut"/>
                                                </EasingColorKeyFrame.EasingFunction>
                                            </EasingColorKeyFrame>
                                        </ColorAnimationUsingKeyFrames>
                                    </Storyboard>
                                </BeginStoryboard.Storyboard>
                            </BeginStoryboard>
                        </DataTrigger.EnterActions>
                        <DataTrigger.ExitActions>
                            <StopStoryboard BeginStoryboardName="sb1"/>
                        </DataTrigger.ExitActions>
                    </DataTrigger>
                    <Trigger Property="IsEnabled" Value="false">
                        <Setter Property="Background" Value="White"/>
                        <Setter Property="Foreground" Value="Black"/>
                    </Trigger>
                </Style.Triggers>
            </Style>
        </ResourceDictionary>
    </UserControl.Resources>

    <Border Style="{StaticResource TopBarCard}">
        <Grid Background="Transparent" Margin="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100"></ColumnDefinition>
                <ColumnDefinition Width="300"></ColumnDefinition>
                <ColumnDefinition Width="1.*"></ColumnDefinition>
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0">
                <Border CornerRadius="8">
                    <Image Source="/WATA.LIS.Main;component/Views/wata_logo.png" Visibility="Visible" Margin="10,2,7,2"/>
                </Border>
            </Grid>

            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"></RowDefinition>
                    <RowDefinition Height="*"></RowDefinition>
                    <RowDefinition Height="*"></RowDefinition>
                    <RowDefinition Height="*"></RowDefinition>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"></ColumnDefinition>
                    <ColumnDefinition Width="1*"></ColumnDefinition>
                </Grid.ColumnDefinitions>
                <Label  Grid.Row="0" Grid.Column="0" Content="Ver :" Style="{StaticResource TopBarKeyLabel}"/>
                <Label  Grid.Row="0" Grid.Column="1" Content="{Binding VersionContext}" Style="{StaticResource TopBarValueLabel}"/>
                <Label  Grid.Row="1" Grid.Column="0" Content="Date :" Style="{StaticResource TopBarKeyLabel}"/>
                <Label  Grid.Row="1" Grid.Column="1" Content="{Binding Date}" Style="{StaticResource TopBarValueLabel}"/>
                <Label  Grid.Row="2" Grid.Column="0" Content="Time :" Style="{StaticResource TopBarKeyLabel}"/>
                <Label  Grid.Row="2" Grid.Column="1" Content="{Binding Time}" Style="{StaticResource TopBarValueLabel}"/>
                <Label  Grid.Row="3" Grid.Column="0" Content="Uptime :" Style="{StaticResource TopBarKeyLabel}"/>
                <Label  Grid.Row="3" Grid.Column="1" Content="{Binding LivingTime}" Style="{StaticResource TopBarValueLabel}"/>
            </Grid>

            <Grid Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"></RowDefinition>
                    <RowDefinition Height="*"></RowDefinition>
                    <RowDefinition Height="*"></RowDefinition>
                    <RowDefinition Height="*"></RowDefinition>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Label  Grid.Row="0" Grid.Column="0" Content="QR Code :"  Style="{StaticResource TopBarKeyLabel}"/>
                <Label  Grid.Row="0" Grid.Column="1" Content="{Binding VisionCamQREvent}" Style="{StaticResource TopBarValueLabel}"/>

                <Label  Grid.Row="1" Grid.Column="0" Content="Item Size :" Style="{StaticResource TopBarKeyLabel}"/>
                <Label  Grid.Row="1" Grid.Column="1" Content="{Binding VisionCamSizeEvent}" Style="{StaticResource TopBarValueLabel}"/>

                <Label  Grid.Row="2" Grid.Column="0" Content="EPC Data :" Style="{StaticResource TopBarKeyLabel}"/>
                <Label  Grid.Row="2" Grid.Column="1" Content="{Binding RFIDEvent}" Style="{StaticResource TopBarValueLabel}"/>

                <Label  Grid.Row="3" Grid.Column="0" Content="Coordinates :" Style="{StaticResource TopBarKeyLabel}"/>
                <Label  Grid.Row="3" Grid.Column="1" Content="{Binding CoordinatesEvent}" Style="{StaticResource TopBarValueLabel}"/>
            </Grid>

            <Grid Grid.Column="3" HorizontalAlignment="Right"/>
        </Grid>
    </Border>
</UserControl>

