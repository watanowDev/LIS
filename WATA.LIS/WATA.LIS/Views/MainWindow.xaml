<Window x:Class="WATA.LIS.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:prism="http://prismlibrary.com/"
        prism:ViewModelLocator.AutoWireViewModel="True"
        xmlns:core="clr-namespace:WATA.LIS.Core;assembly=WATA.LIS.Core"
        Title="WATA LIS" Height="780" Topmost="False" Width="1700" WindowStyle="ToolWindow"   Left="900" Top="100">
    <Grid Background="#EDF1F2">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" BorderBrush="#00000000" BorderThickness="0">
            <ContentControl prism:RegionManager.RegionName="{x:Static core:RegionNames.TopStatusBar}" />
        </Border>

        <!-- Persistent navigation bar (using BottomUIRegion) -->
        <Border Grid.Row="1" BorderBrush="#00000000" BorderThickness="0">
            <ContentControl prism:RegionManager.RegionName="{x:Static core:RegionNames.BottomUIRegion}" />
        </Border>

        <!-- Main content region with rounded container -->
        <Border Grid.Row="2" CornerRadius="10" Background="#FFFFFF" Margin="8,6,8,6" Padding="8,6,8,6">
            <Border.Effect>
                <DropShadowEffect BlurRadius="10" Direction="270" ShadowDepth="2" Opacity="0.08"/>
            </Border.Effect>
            <ContentControl prism:RegionManager.RegionName="{Binding MainRegionName}" />
        </Border>
    </Grid>
</Window>
